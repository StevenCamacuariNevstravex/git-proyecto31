cd D: (ingresar disco D)
ls (monstrar contenido de carpeta)
cd XAMPP
cd htdocs
ls
cd 31
git init (crear git repository)
git status (estan workind direct lo que quiero)
"agrego los archivos al stating area"
git add app.js (mover a stating area) uno por uno
git add . (para pasar todos los archivos)  todos los archivos

git commit

"ante de escribir mensaje"
i  "luego lo borras"
escribir mensaje : 
luego de escribir el mensaje darle "esc"
hola : wq
git log (para ver los inserts que hemos echo)

//
para elimina algun cambio dentro del programa visual
git checkout -- index.html